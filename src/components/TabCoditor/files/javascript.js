// traditional loop
let str = "hello";
for (let i = 0; i < str.length; i++) {
  console.log(str.charAt[i]);
}

//any iterable types: array, Map, Set, String
let arr = ["apple", "orange"];
for (let item of iterable) {
  // item
}

// for in
for (prop in object) { ...
}

// functional
arr.forEach(item => console.log(item);